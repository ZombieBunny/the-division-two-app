import { GearSlotFilterPipe } from './gear-slot-filter.pipe';

describe('GearSlotFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GearSlotFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
