import { GearTalentFilterPipe } from './gear-talent-filter.pipe';

describe('GearTalentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GearTalentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
