import { GearAttributeFilterPipe } from './gear-attribute-filter.pipe';

describe('GearAttributeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GearAttributeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
